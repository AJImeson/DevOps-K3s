{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Values.prefix }}
  ports:
    - port: 80
      targetPort: 80
{{- end }}

